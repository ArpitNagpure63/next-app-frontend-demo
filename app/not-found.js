import styles from './page.module.css'

const NotFound = () => {
    return <div className={styles.maincontainer}>
        <h1 className={styles.heading}>This is not available on this Next App</h1>
    </div>
}

export default NotFound;